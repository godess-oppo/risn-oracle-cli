{
  "task": "policy",
  "rules": [
    "All destructive actions must be dry-run by default",
    "Reversible plans must be written to risn/actions/*.json",
    "Unsafe content must be blocked unless --policy-accept is used",
    "All actions must be logged to risn/audit.log"
  ]
}
